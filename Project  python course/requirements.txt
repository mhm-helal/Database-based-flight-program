# متطلبات مشروع حجز رحلات الطيران
# Flight Reservation System Requirements

# =====================================================
# المكتبات المدمجة في Python - لا تحتاج تثبيت
# Built-in Python libraries - No installation needed
# =====================================================

# tkinter        - واجهة المستخدم الرسومية (GUI)
# sqlite3        - قاعدة البيانات المحلية
# os             - عمليات نظام التشغيل
# sys            - إعدادات النظام

# =====================================================
# متطلبات النظام
# System Requirements
# =====================================================

# Python 3.6+    - إصدار بايثون المطلوب
# Windows/Mac/Linux - أنظمة التشغيل المدعومة

# =====================================================
# مكتبات اختيارية للتطوير والتصدير
# Optional libraries for development and deployment
# =====================================================

# للتصدير كملف تنفيذي (اختياري):
# For creating executable file (optional):
# pyinstaller>=4.0

# للتطوير والاختبار (اختياري):
# For development and testing (optional):
# pytest>=6.0
# black>=21.0
# flake8>=3.8

# =====================================================
# تعليمات التثبيت
# Installation Instructions
# =====================================================

# 1. تأكد من تثبيت Python 3.6 أو أحدث
# 2. جميع المكتبات المطلوبة مدمجة في Python
# 3. لا حاجة لتشغيل pip install
# 4. فقط شغل: python main.py

# 1. Make sure Python 3.6+ is installed
# 2. All required libraries are built into Python
# 3. No need to run pip install
# 4. Just run: python main.py

# =====================================================
# تثبيت PyInstaller للتصدير (اختياري)
# Install PyInstaller for exe export (optional)
# =====================================================

# pip install pyinstaller
# pyinstaller --onefile --windowed main.py